# 为 RegistrarLens 贡献

感谢你帮助改进 RegistrarLens。小而清晰、带测试的改动最容易审查和合并。

## 开始之前

- 功能建议和普通缺陷可以先提交 Issue，说明输入、预期结果与实际结果。
- 安全问题不要公开披露，请遵循 [SECURITY.md](SECURITY.md)。
- 较大的界面、数据流或架构改动，建议先在 Issue 中讨论，避免重复劳动。

## 本地开发

需要 Node.js 22 或更高版本。项目运行时零依赖，因此无需安装 npm 包。

```bash
git clone <your-fork-url>
cd registrar-lens
npm test
python -m http.server -d public 8000
```

打开 `http://localhost:8000` 查看页面。也可以用 `npx serve public` 启动静态服务器。

## 开发原则

- 保持静态、无需 API Key、无需项目自有后端的核心架构。
- 不添加遥测、用户账号或服务端查询历史。
- 新增第三方依赖前说明必要性、体积、维护状态和隐私影响；能用平台 API 清楚实现时优先不加依赖。
- 不把用户输入直接拼入 HTML；所有外部 RDAP 数据都应视为不可信输入。
- 批量查询要控制并发，并尊重上游服务的限速和失败响应。
- 不宣称覆盖全部 TLD。对 ccTLD、缺少 IANA ID、CORS 和上游异常给出可理解的失败状态。
- 保持键盘可操作性、清晰焦点样式和足够的颜色对比度。

## 测试

提交前运行：

```bash
npm test
```

测试应确定、快速且可离线运行。RDAP 解析测试请使用最小化的固定 fixture，不要让 CI 依赖实时网络服务。修复缺陷时，尽可能先加入能复现问题的测试。

## 提交 Pull Request

1. 从最新 `main` 创建短生命周期分支。
2. 只包含一个主题相关的改动，并补齐相应测试和文档。
3. 确认 `npm test` 通过，且在窄屏与桌面浏览器中做过基本检查。
4. 在 PR 中说明动机、实现方式、验证结果和已知取舍；界面改动请附截图。

提交贡献即表示你同意按本项目的 [Apache License 2.0](LICENSE) 许可该贡献。
