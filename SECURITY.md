# 安全政策

## 支持范围

RegistrarLens 当前没有独立的版本发布分支。安全修复只应用于 `main` 的最新代码和由它部署的站点；旧提交、个人分支与第三方部署不在维护范围内。

## 报告漏洞

请不要在公开 Issue、讨论区或 Pull Request 中披露尚未修复的漏洞。

优先使用仓库 **Security → Report a vulnerability** 创建私密安全报告，并提供：

- 受影响的页面、文件或提交；
- 可重复的最小步骤或概念验证；
- 实际影响与利用前提；
- 你建议的修复方向（如有）。

如果仓库尚未启用私密漏洞报告，请只创建一个不含漏洞细节的 Issue，请求维护者提供私下联系方式。

维护者会尽量在 7 天内确认收到报告，并在完成影响评估后同步修复计划。请在修复发布或双方约定的披露日期之前保密。

## 安全边界

RegistrarLens 是静态浏览器应用，没有项目自有的查询后端。浏览器会直接连接 IANA 和第三方 RDAP 服务，因此：

- 上游返回的内容必须按不可信数据处理；
- 第三方服务可以看到访问者 IP、查询域名及常规 HTTP 元数据；
- CORS、限速、服务可用性和上游数据准确性不受本项目控制；
- 仿冒部署、被修改的第三方副本以及浏览器扩展造成的问题不属于本仓库漏洞。

仍欢迎报告可能导致脚本执行、敏感数据泄露、依赖或工作流供应链风险，以及绕过浏览器端安全控制的问题。
